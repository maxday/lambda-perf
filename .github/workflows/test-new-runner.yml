name: test build arm on macos-14

on:
  push:
    
jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v3
      - uses: douglascamata/setup-docker-macos-action@v1-alpha.11
      - name: Build
        run: cd s3-uploader/runtimes/rust_on_provided_al2023 && docker build .
      # - name: test build arm
      #   run: cd s3-uploader/runtimes/rust_on_provided_al2023 && docker buildx build .
      # /Applications/Docker.app/Contents/MacOS/com.docker.socket




